# Startup yields during preheat; virtual SD is held until continuation succeeds.
[gcode_macro PRINT_START]
description: Validate, prepare, and start interruptible preheat
gcode: !!include python/print_start.py

[gcode_macro _PRINT_START_STATE]
variable_active: False
variable_sd_paused: False
variable_filename: ''
variable_params: {}
gcode:

[gcode_macro _PRINT_START_BEGIN]
description: Start the print
gcode:
  {% set svv = printer.save_variables.variables %}
  {% set vars = printer["gcode_macro _PRINTER_VARS"] %}
  {% set bed_temp = params.BED_TEMP|default(0)|float %}
  {% set hotend_temp = params.HOTEND_TEMP|default(0)|float %}
  {% set chamber_temp = params.TARGET_CHAMBER_TEMP|default(0)|float %}
  {% set material = params.MATERIAL | default(vars.default_material)|upper %}
  {% set initial_tool = params.TOOL|default("0")|int %}
  {% set travel_accel = vars.travel_accel|float %}
  {% set saved_accel = printer.configfile.settings.printer.max_accel %}
  {% set contact_cal_temp = printer["gcode_macro BEACON_VARS"].beacon_contact_calibration_temp|int %}

  UPDATE_DELAYED_GCODE ID=FILTER_DELAYED_STOP DURATION=0
  _CLEAR_PRINT_SKEW

  # Fire heaters immediately (non-blocking) so bed heating overlaps homing and motor sync
  {% if bed_temp > 0 %}
    M140 S{bed_temp}
  {% endif %}
  M104 S{contact_cal_temp}

  # Guard against a prior/cancelled print leaving limits raised (esp. SCV) before homing/tilt/mesh
  RESET_VELOCITY_LIMITS

  MAYBE_HOME
  # Skip sync when phases are still applied and motors were never disabled since the last home
  {% if not (printer.motors_sync is defined and printer.motors_sync.applied and 'xyz' in printer.toolhead.homed_axes) %}
    SYNC_MOTORS ACCEL_CHIP=beacon
  {% else %}
    RESPOND MSG="Motor sync still valid - skipping"
  {% endif %}

  SAVE_VARIABLE VARIABLE=current_material VALUE='"{material}"'

  CLEAR_PAUSE
  RESET_MULTIPLIERS

  G90

  BED_MESH_CLEAR
  SET_GCODE_OFFSET Z=0

  _BEACON_SET_NOZZLE_TEMP_OFFSET RESET=True

[gcode_macro _PRINT_START_AFTER_PREHEAT]
gcode:
  {% set params = printer['gcode_macro _PRINT_START_STATE'].params %}
  {% set vars = printer['gcode_macro _PRINTER_VARS'] %}
  {% set hotend_temp = params.HOTEND_TEMP|float %}
  {% set initial_tool = params.TOOL|default(0)|int %}
  {% set material = params.MATERIAL|default(vars.default_material)|upper %}
  {% set travel_accel = vars.travel_accel|float %}
  {% set saved_accel = printer.toolhead.max_accel %}
  {% set contact_cal_temp = printer['gcode_macro BEACON_VARS'].beacon_contact_calibration_temp|int %}
  # Calibrate z offset with Beacon contact (must be done hot, after PREHEAT)
  G28 Z METHOD=CONTACT CALIBRATE=1

  # Level gantry
  Z_TILT_ADJUST

  # KAMP or default Beacon mesh, with quality check
  CALIBRATE_BED_MESH

  # Clean nozzle after mesh, then re-home Z for final offset
  {% if printer.configfile.config["gcode_macro CLEAN_NOZZLE"] is defined %}
    CLEAN_NOZZLE SKIP_HEATING=1
  {% endif %}
  M104 S{contact_cal_temp}
  TEMPERATURE_WAIT SENSOR=extruder MINIMUM={contact_cal_temp - 2}
  G28 Z METHOD=CONTACT CALIBRATE=0
  M104 S{hotend_temp}
  SET_VELOCITY_LIMIT ACCEL={travel_accel}

  # Park toolhead near print area
  {% if printer.configfile.config["gcode_macro SMART_PARK"] is defined %}
      SMART_PARK
  {% else %}
      PICK_PARK_LOCATION
  {% endif %}

  _SET_MPC_MATERIAL MATERIAL="{material}"

  SET_DISPLAY_TEXT MSG="Heating hotend to {hotend_temp}c"
  M109 S{hotend_temp}

  {% if "T0" in printer.gcode.commands and (printer.mmu is not defined or printer.mmu.enabled) %}
    SET_DISPLAY_TEXT MSG="Loading tool T{initial_tool}"
    T{initial_tool}
  {% endif %}

  _PRINT_START_CHECK_READY

  # Park again after potential toolchange
  {% if printer.configfile.config["gcode_macro SMART_PARK"] is defined %}
      SMART_PARK
  {% else %}
      PICK_PARK_LOCATION
  {% endif %}

  # Apply thermal expansion compensation
  {% if printer.configfile.config["gcode_macro _BEACON_SET_NOZZLE_TEMP_OFFSET"] is defined %}
    RESPOND MSG="Setting Beacon thermal expansion compensation"
    _BEACON_SET_NOZZLE_TEMP_OFFSET
  {% endif %}

  # Purge nozzle before printing
  {% if printer.configfile.config["gcode_macro LINE_PURGE"] is defined %}
    LINE_PURGE
  {% else %}
    _FALLBACK_PURGE
  {% endif %}

  SET_VELOCITY_LIMIT ACCEL={saved_accel}

  MAYBE_LOAD_SKEW_CORRECTION

  RESPOND MSG="Starting Print..."
  SAVE_VARIABLE VARIABLE=is_printing_gcode VALUE=True

# =====================
# PRINT_END (unit-safe; avoids duplicate M84)
# =====================
[gcode_macro PRINT_END]
description: Consistent end-of-print routine. REASON must be 'complete' or 'cancel'
gcode:
  {% set reason = params.REASON|default("complete")|lower %}
  {% if reason not in ["complete", "cancel"] %}
    { action_raise_error("PRINT_END: REASON must be 'complete' or 'cancel' (got '%s')" % reason) }
  {% endif %}
  {% set quick_cooldown = 0 if reason == "complete" else 1 %}

  {% set vars = printer["gcode_macro _PRINTER_VARS"] %}
  {% set retract_speed = (vars.retract_speed * 60)|int %}
  {% set retract_length = vars.end_retract_length|float %}

  M400

  TURN_PART_COOLING_FAN_OFF

  G92 E0
  {% if printer.extruder.can_extrude %}
    G91
    G1 E-{retract_length} F{retract_speed}
    G90
  {% endif %}

  _CLEAR_PRINT_SKEW
  PICK_PARK_LOCATION

  {% if printer.configfile.config["gcode_macro _BEACON_REMOVE_NOZZLE_TEMP_OFFSET"] is defined %}
    _BEACON_REMOVE_NOZZLE_TEMP_OFFSET
  {% endif %}

  {% if "bed_mesh" in printer.configfile.settings %}
    BED_MESH_CLEAR
  {% endif %}

  COOLDOWN_SEQUENCE QUICK={quick_cooldown}
  {% if reason == "cancel" %}
    DISABLE_ALL_MOTORS
  {% else %}
    RESPOND MSG="Steppers left enabled for quick restart - idle timeout will release them"
  {% endif %}

  RESET_TO_START_STATE


[gcode_macro PAUSE]
rename_existing: PAUSE_BASE
gcode:
  {% if printer['gcode_macro _PRINT_START_STATE'].active %}
    {action_raise_error("Startup is heating; use TA_CHAMBER_STOP or CANCEL_PRINT to stop")}
  {% endif %}
  {% if printer.pause_resume.is_paused %}
    {action_respond_info("Already paused...")}
  {% else %}

    {% set vars = printer["gcode_macro _PRINTER_VARS"] %}
    {% set mmu_active = true if printer.mmu is defined and printer.mmu.enabled else false %}
    {% set retract_speed = (vars.retract_speed * 60)|int %}
    {% set retract_length = vars.end_retract_length|float %}


    {% if mmu_active %}
      _MMU_SAVE_POSITION   ; HH plumbing (mmu_sequence.cfg symlink) - idempotent, wiki-required for custom PAUSE
    {% endif %}

    SET_GCODE_VARIABLE MACRO=RESUME VARIABLE=printing_hotend_temp VALUE={printer['extruder'].target}
    PAUSE_BASE

    {% if not mmu_active %}
      SET_GCODE_VARIABLE MACRO=RESUME VARIABLE=retracted_length VALUE={retract_length if printer.extruder.can_extrude else 0}
      {% if printer.extruder.can_extrude %}
        SAVE_GCODE_STATE NAME=_PAUSE_EXTRUSION
        M83
        G1 E-{retract_length} F{retract_speed}
        RESTORE_GCODE_STATE NAME=_PAUSE_EXTRUSION
      {% endif %}
      TURN_PART_COOLING_FAN_OFF
      PICK_PARK_LOCATION
    {% endif %}

    SAVE_GCODE_STATE NAME=PAUSEPARK

    {% if printer['extruder'].target > 0 %}
      {% set standby_temp = [(printer['extruder'].target - vars.pause_standby_extruder_delta)|int, vars.pause_min_standby_temp|int]|max %}
      M104 S{standby_temp}
      RESPOND MSG="Hotend set to standby: {standby_temp}C"
    {% endif %}

    SET_IDLE_TIMEOUT TIMEOUT={vars.pause_idle_timeout}
    RESPOND MSG="Setting idle timeout to {vars.pause_idle_timeout} seconds."
  {% endif %}


[gcode_macro RESUME]
rename_existing: RESUME_BASE
variable_printing_hotend_temp: 0
variable_retracted_length: 0
gcode:
  {% if printer['gcode_macro _PRINT_START_STATE'].active %}
    {action_raise_error("Startup is heating; use TA_CHAMBER_STOP or CANCEL_PRINT to stop")}
  {% endif %}
  {% if not printer.pause_resume.is_paused %}
    {action_raise_error("RESUME: printer is not paused")}
  {% endif %}
  {% if printing_hotend_temp > 0 %}
    M109 S{printing_hotend_temp}
  {% endif %}
  {% if printer.mmu is defined and printer.mmu.enabled %}
    # Rat Race's validated HH integration explicitly restores after reheating.
    _MMU_RESTORE_POSITION
    {% if printer.configfile.config["gcode_macro _BEACON_SET_NOZZLE_TEMP_OFFSET"] is defined %}
      _BEACON_SET_NOZZLE_TEMP_OFFSET
    {% endif %}
    SET_IDLE_TIMEOUT TIMEOUT={printer.configfile.settings.idle_timeout.timeout|int}
    RESUME_BASE
  {% else %}
    _RESUME_NON_MMU
  {% endif %}

[gcode_macro _RESUME_NON_MMU]
description: Recheck extrusion after reheat, then let firmware restore the print position once
gcode:
  {% set vars = printer["gcode_macro _PRINTER_VARS"] %}
  {% set resume = printer["gcode_macro RESUME"] %}
  {% if printer['gcode_macro _PRINT_START_STATE'].active %}
    {action_raise_error("Startup is heating; use TA_CHAMBER_STOP or CANCEL_PRINT to stop")}
  {% endif %}
  {% if not printer.pause_resume.is_paused %}
    {action_raise_error("RESUME: printer is not paused")}
  {% endif %}
  {% if resume.retracted_length|float > 0 and not printer.extruder.can_extrude %}
    {action_raise_error("RESUME: nozzle is still too cold to unretract")}
  {% endif %}
  {% set saved_accel = printer.toolhead.max_accel %}
  SET_VELOCITY_LIMIT ACCEL={vars.travel_accel|float}
  RESTORE_GCODE_STATE NAME=PAUSEPARK MOVE=1 MOVE_SPEED={vars.travel_speed|float / 2}
  {% if printer.configfile.config["gcode_macro _BEACON_SET_NOZZLE_TEMP_OFFSET"] is defined %}
    _BEACON_SET_NOZZLE_TEMP_OFFSET
  {% endif %}
  {% if resume.retracted_length|float > 0 %}
    SAVE_GCODE_STATE NAME=_RESUME_EXTRUSION
    M83
    G1 E{resume.retracted_length|float} F{vars.retract_speed|float * 60}
    RESTORE_GCODE_STATE NAME=_RESUME_EXTRUSION
    SET_GCODE_VARIABLE MACRO=RESUME VARIABLE=retracted_length VALUE=0
  {% endif %}
  SET_IDLE_TIMEOUT TIMEOUT={printer.configfile.settings.idle_timeout.timeout|int}
  RESUME_BASE VELOCITY={vars.travel_speed|float / 2}
  SET_VELOCITY_LIMIT ACCEL={saved_accel}

[gcode_macro CANCEL_PRINT]
rename_existing: BASE_CANCEL_PRINT
description: Cancel print
gcode:
    # Mandatory cancellation is independent of parking, homing and optional hardware.
    TURN_OFF_HEATERS_BASE
    BASE_CANCEL_PRINT
    _TA_CHAMBER_RESET
    M107
    M84
    SET_IDLE_TIMEOUT TIMEOUT={printer.configfile.settings.idle_timeout.timeout|int}
    SET_GCODE_VARIABLE MACRO=RESUME VARIABLE=printing_hotend_temp VALUE=0
    SET_GCODE_VARIABLE MACRO=RESUME VARIABLE=retracted_length VALUE=0
    _CLEAR_PRINT_SKEW
    RESET_TO_START_STATE
    {% if "bed_mesh" in printer.configfile.settings %}
        BED_MESH_CLEAR
    {% endif %}
    {% if printer.configfile.config["gcode_macro _BEACON_REMOVE_NOZZLE_TEMP_OFFSET"] is defined %}
        _BEACON_REMOVE_NOZZLE_TEMP_OFFSET
    {% endif %}
    {% if printer.mmu is defined and printer.mmu.enabled %}
        MMU_TTG_MAP RESET=1 QUIET=1
    {% endif %}
    COOLDOWN_SEQUENCE QUICK=1
    {% if "CASE_LIGHTS_OFF" in printer.gcode.commands %} CASE_LIGHTS_OFF {% endif %}
    RESPOND MSG="Print cancelled; heaters and motors off."

[gcode_macro M600]
description: Filament change (pause + unload)
gcode:
	{% set vars = printer["gcode_macro _PRINTER_VARS"] %}
	{% set unload_len = vars.m600_unload_length|float %}
	{% set unload_feedrate = (vars.m600_unload_speed|float * 60)|int %}
	PAUSE
	{% if printer.mmu is defined and printer.mmu.enabled %}
	  MMU_PAUSE
	{% elif printer.configfile.config["gcode_macro UNLOAD_FILAMENT"] is defined %}
	  UNLOAD_FILAMENT
	{% elif printer.extruder.can_extrude %}
	  G91
	  G1 E-{unload_len} F{unload_feedrate}
	  G90
	{% else %}
	  RESPOND MSG="No filament system detected — manual change required"
	{% endif %}